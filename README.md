# Agenda Blindada — SaaS MVP (PT-PT)
Next.js + Supabase + WhatsApp Cloud API

## Setup
1) Supabase: correr `supabase/schema.sql`
2) `.env.local`: copiar de `.env.example`
3) `npm install` e `npm run dev`
4) WhatsApp webhook: `/api/whatsapp/webhook` (GET verify + POST messages)

## Jobs
- `npm run reminders:24h`
- `npm run thanks`
- `npm run rebook`
